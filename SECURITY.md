# 安全策略

## 负责任披露

若你发现与安全相关的问题（如敏感信息泄露、认证绕过、注入等），请**不要**在公开 Issue 中直接披露细节。

请通过以下方式之一联系维护者：

- 在仓库中**新建一个非公开的 Security Advisory**（GitHub：Security → Advisories → New draft），或
- 通过可接受的私下渠道（如仓库说明中的联系邮箱）说明问题概要与复现步骤。

我们会在确认并完成修复（或缓解）后，再与你协商是否以及如何公开致谢与漏洞细节。

## 安全相关约定

- 本仓库**不**接受包含真实 API Key、token、密码或其它敏感信息的提交。
- 配置与密钥分离：敏感信息仅通过环境变量（如 `env:BAILIAN_API_KEY`）或 `.env` 提供，且 `.env` 已列入 `.gitignore`。
- 若你曾误提交过密钥，请立即在对应服务中轮换该密钥，并考虑从 Git 历史中移除敏感内容。

感谢你帮助保持本项目与用户环境的安全。
